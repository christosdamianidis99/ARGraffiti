# ARGraffiti

## Build Notes
- Removed the legacy Google Play Services Resolver editor plugin (PlayServicesResolver folder) because it referenced a missing `Google.EditorMeasurement` type and triggered `TypeLoadException` during builds.
